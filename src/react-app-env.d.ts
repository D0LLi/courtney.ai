```js
import { GLView } from 'react-native-reanimated';

const MyComponent = () => {
  return <GLView />;
};
```
```js
import { GLView } from 'react-native-reanimated';

const MyComponent = () => {
  return <GLView />;
};
```
import { Constants } from 'expo';

const { manifest } = Constants;

const {
  name,
  version,
  id,
  orientation,
  platform,
  statusBarHeight,
  deviceName,
  deviceYearClass,
  isDevice,
  isPad,
  isTV,
  systemVersion,
  expoVersion,
  supportedExpoSdks,
  expoRuntimeVersion,
  debugMode,
  isDetached,
  appOwnership,
  linkingUri,
  sessionId,
  installationId,
  releaseChannel,
  nativeAppVersion,
  nativeBuildVersion,
} = manifest;
```
```ts
import { Constants } from 'expo';

const { manifest } = Constants;

const {
  name,
  version,
  id,
  orientation,
  platform,
  statusBarHeight,
  deviceName,
  deviceYearClass,
  isDevice,
  isPad,
  isTV,
  systemVersion,
  expoVersion,
  supportedExpoSdks,
  expoRuntimeVersion,
  debugMode,
  isDetached,
  appOwnership,
  linkingUri,
  sessionId,
  installationId,
  releaseChannel,
  nativeAppVersion,
  nativeBuildVersion,
} = manifest;
```
